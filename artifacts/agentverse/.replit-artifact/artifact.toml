kind = "web"
previewPath = "/"
title = "AgentVerse"
version = "1.0.0"
id = "artifacts/agentverse"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23593

[services.development]
run = "pnpm --filter @workspace/agentverse run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agentverse", "run", "build" ]
publicDir = "artifacts/agentverse/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23593"
BASE_PATH = "/"
